<div class="w-full max-w-7xl mt-2 mx-auto flex mb-2">
  <div>
    <h5 class="font-bold text-5xl text-indigo-400">Soundcloud</h5>
  </div>
   <% if current_user %>
    <div class="pt-2 ml-auto mt-2">
      <%= link_to "Sign Out", destroy_user_session_path, class: "bg-purple-500 text-white px-5 py-3 rounded-lg w-32 text-center font-semibold",
      data: { turbo_method: :delete } %>
    </div>
  <% end %>
</div>

<div class="w-full max-w-8xl h-px bg-gray-200 my-4 mx-auto"></div>
